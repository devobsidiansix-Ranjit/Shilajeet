version: "3.8"

services:

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: shilajit-frontend:latest
    networks:
      - public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`shilajit.apasya.in`)" # root domain
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls.certresolver=myresolver"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: shilajit-backend:latest
    environment:
      - NODE_ENV=production
    expose:
      - "5000" # internal exposure for Traefik
    networks:
      - public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend.rule=Host(`api.shilajit.apasya.in`)"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.tls.certresolver=myresolver"
      - "traefik.http.services.backend.loadbalancer.server.port=5000"

networks:
  public:
    driver: bridge

volumes:
  letsencrypt:
